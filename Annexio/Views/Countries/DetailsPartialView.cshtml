@model Annexio.Models.Country

<ul class="nav nav-tabs">
    <li class="active"><a href="#country" data-toggle="tab" aria-expanded="true">Country</a></li>
    <li class=""><a href="#borders" data-toggle="tab" aria-expanded="false">Borders</a></li>

</ul>
<div class="tab-content">
    <div class="tab-pane fade active in" id="country">
        <h1>@Html.DisplayFor(model => model.Name)</h1>
        <dl>
            <dt>
                @Html.DisplayNameFor(model => model.Capital)
            </dt>
            <dd>
                @Html.DisplayFor(model => model.Capital)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.Population)
            </dt>
            <dd>
                @Html.DisplayFor(model => model.Population)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.Currencies)
            </dt>
            @foreach (var c in Model.Currencies)
            {
                <dd>
                    @Html.DisplayFor(model => c.Name)
                </dd>
            }

            <dt>
                @Html.DisplayNameFor(model => model.Languages)
            </dt>
            @foreach (var l in Model.Languages)
            {
                <dd>
                    @Html.DisplayFor(model => l.Name)
                </dd>
            }
        </dl>
    </div>

    <div class="tab-pane fade" id="borders">
        <ul class="nav nav-pills">
            @foreach (var b in Model.Borders)
            {
                <li>
                    @Html.ActionLink(b, "DetailsByCode", new { code = b })
                </li>
            }
        </ul>
    </div>
</div>




